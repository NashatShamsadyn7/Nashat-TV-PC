appId: tv.nashat.pc
productName: Nashat TV
copyright: Copyright © 2026 Nashat Shamsadyne

directories:
  output: dist
  buildResources: build

files:
  - out/**/*
  - package.json
  - "!**/node_modules/*/{CHANGELOG.md,README.md,readme.md,changelog.md}"
  - "!**/{.DS_Store,.git,.gitignore,.eslintrc*,.prettierrc*}"

asar: true
asarUnpack:
  - resources/**

win:
  target:
    - target: nsis
      arch: [x64]
  # Spelled out rather than ${productName}: that expands to "Nashat TV", and the
  # space becomes %20 in the download URL while breaking continuity with the
  # Nashat-TV-Setup-*.exe names every published release already uses.
  artifactName: Nashat-TV-Setup-${version}.${ext}

nsis:
  oneClick: false
  perMachine: false
  allowToChangeInstallationDirectory: true
  createDesktopShortcut: true
  createStartMenuShortcut: true
  shortcutName: Nashat TV

mac:
  target:
    - target: dmg
      arch: [x64, arm64]
  category: public.app-category.entertainment

linux:
  target:
    - AppImage
    - deb
  category: AudioVideo

publish:
  provider: github
  owner: NashatShamsadyn7
  repo: Nashat-TV-PC
  releaseType: release
